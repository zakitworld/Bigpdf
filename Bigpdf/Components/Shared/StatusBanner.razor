@if (!string.IsNullOrWhiteSpace(Message))
{
    <div class="status-banner @(Success ? "status-success" : "status-error")">
        @Message
    </div>
}

@code {
    [Parameter] public string? Message { get; set; }
    [Parameter] public bool Success { get; set; }
}
